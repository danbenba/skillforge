---
name: xml-description-skill
description: This skill embeds an XML tag such as the <tool> element inside its description, which is forbidden because frontmatter is injected into the system prompt, so the validator should flag the description format as containing angle brackets.
version: 1.0.0
---

## Instructions

This fixture exercises the description XML-tag check.
